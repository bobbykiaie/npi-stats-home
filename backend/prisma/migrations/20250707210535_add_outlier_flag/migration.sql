BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[inspectionLogs] ADD [is_outlier] BIT NOT NULL CONSTRAINT [inspectionLogs_is_outlier_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
