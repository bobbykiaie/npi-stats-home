BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[activeBuilds] DROP CONSTRAINT [activeBuilds_username_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
